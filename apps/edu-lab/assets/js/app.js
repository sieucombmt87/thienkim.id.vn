const DB_KEY='TK_EDU_LAB_DB_V11';
const routerMap={
  math:{label:'Toán',portal:'Học hỏi / Bài tập',module:'exercises/math',path:'/hoctap/lop-3/toan/',cat:'Bài tập'},
  vietnamese:{label:'Tiếng Việt',portal:'Học hỏi / Bài tập',module:'exercises/vietnamese',path:'/hoctap/lop-3/tieng-viet/',cat:'Bài tập'},
  english:{label:'Tiếng Anh',portal:'Học hỏi / Bài tập',module:'exercises/english',path:'/hoctap/lop-3/tieng-anh/',cat:'Bài tập'},
  reading:{label:'Đọc hiểu',portal:'Học hỏi / Đọc • Nghe • Viết',module:'reading-listening-writing/reading',path:'/hoctap/lop-3/doc-hieu/',cat:'Đọc • Nghe • Viết'},
  vocabulary:{label:'Từ vựng',portal:'Học hỏi / Từ vựng',module:'vocabulary/flashcards',path:'/hoctap/lop-3/tu-vung/',cat:'Từ vựng'},
  logic:{label:'Tư duy logic',portal:'Học hỏi / Bài tập',module:'exercises/logic',path:'/hoctap/lop-3/logic/',cat:'Bài tập'},
  life_skill:{label:'Kỹ năng sống',portal:'Học hỏi / Giáo lý & đời sống',module:'doctrine-life/life-skills',path:'/hoctap/8-12/ky-nang-song/',cat:'Giáo lý & đời sống'},
  doctrine:{label:'Đạo đức / đời sống',portal:'Học hỏi / Giáo lý & đời sống',module:'doctrine-life/ethics',path:'/hoctap/8-12/dao-duc-doi-song/',cat:'Giáo lý & đời sống'}
};
const seed={
  students:[
    {id:'stu_tk',name:'Thiên Kim',pin:'1234',age:8,grade:'Lớp 3'},
    {id:'stu_001',name:'Bạn An',pin:'1234',age:8,grade:'Lớp 3'},
    {id:'stu_002',name:'Bạn Bin',pin:'1234',age:9,grade:'Lớp 3'},
    {id:'stu_003',name:'Bạn Su',pin:'1234',age:8,grade:'Lớp 3'},
    {id:'stu_004',name:'Bạn Kem',pin:'1234',age:10,grade:'Lớp 4'}
  ],
  lessons:[
    {id:'les_001',title:'Biết nói lời cảm ơn',subject:'life_skill',cat:'Đời sống',level:'Dễ',desc:'Câu chuyện ngắn giúp bé hiểu khi nào nên cảm ơn.',path:'/hoctap/8-12/ky-nang-song/biet-noi-loi-cam-on/'},
    {id:'les_002',title:'Đọc kỹ đề trước khi làm toán',subject:'math',cat:'Kỹ năng học',level:'Vừa',desc:'Rèn thói quen gạch chân dữ kiện quan trọng.',path:'/hoctap/lop-3/toan/doc-ky-de-truoc-khi-lam-toan/'},
    {id:'les_003',title:'Từ vựng chủ đề trường học',subject:'vocabulary',cat:'Từ vựng',level:'Dễ',desc:'Ôn các từ quen thuộc trong lớp học.',path:'/hoctap/lop-3/tu-vung/truong-hoc/'}
  ],
  questions:[
    {id:'q1',topic:'Đời sống',level:'easy',question:'Khi được người khác giúp đỡ, em nên làm gì?',options:[{id:'A',text:'Im lặng bỏ đi'},{id:'B',text:'Nói lời cảm ơn'},{id:'C',text:'Tức giận'},{id:'D',text:'Không quan tâm'}],correct:'B',explain:'Nói lời cảm ơn là cách thể hiện sự lễ phép.'},
    {id:'q2',topic:'Toán',level:'easy',question:'245 - 128 bằng bao nhiêu?',options:[{id:'A',text:'117'},{id:'B',text:'127'},{id:'C',text:'137'},{id:'D',text:'147'}],correct:'A',explain:'245 - 128 = 117.'},
    {id:'q3',topic:'Kỹ năng học',level:'medium',question:'Trước khi làm bài toán có lời văn, em nên làm gì trước?',options:[{id:'A',text:'Chọn đáp án thật nhanh'},{id:'B',text:'Đọc kỹ đề và tìm dữ kiện'},{id:'C',text:'Bỏ qua câu hỏi'},{id:'D',text:'Hỏi bạn đáp án'}],correct:'B',explain:'Đọc kỹ đề giúp mình hiểu bài và tránh sai do vội.'},
    {id:'q4',topic:'Từ vựng',level:'easy',question:'“School” nghĩa là gì?',options:[{id:'A',text:'Trường học'},{id:'B',text:'Cửa hàng'},{id:'C',text:'Công viên'},{id:'D',text:'Nhà bếp'}],correct:'A',explain:'School nghĩa là trường học.'}
  ],
  tests:[{id:'test_001',title:'Bài test khởi động lớp 3: Học hỏi mỗi ngày',duration:30,questions:['q1','q2','q3','q4'],shuffleQuestions:true,shuffleOptions:true,maxAttempts:10}],
  reviews:[
    {id:'rv1',title:'245 - 128 = ?',subject:'math',path:'/hoctap/lop-3/toan/cong-tru-trong-pham-vi-1000/',question:'Một cửa hàng có 245 quyển vở, bán 128 quyển. Còn lại bao nhiêu?',ai:'A',parent:'A',admin:'A',assistant:'A',note:'Đã thống nhất: 245 - 128 = 117.'},
    {id:'rv2',title:'Hành vi khi người lạ rủ đi theo',subject:'life_skill',path:'/hoctap/8-12/ky-nang-song/khong-di-theo-nguoi-la/',question:'Nếu có người lạ rủ em đi theo, em nên làm gì?',ai:'C',parent:'C',admin:'',assistant:'C',note:'Thiếu xác nhận admin nên màu vàng.'},
    {id:'rv3',title:'Từ chỉ hoạt động',subject:'vietnamese',path:'/hoctap/lop-3/tieng-viet/tu-chi-hoat-dong/',question:'Trong các từ: cái bàn, chạy, xanh, quyển vở — từ nào chỉ hoạt động?',ai:'B',parent:'B',admin:'C',assistant:'B',note:'Có xung đột: admin chọn C trong khi AI/phụ huynh/assistant chọn B. Màu đỏ, chưa cho xuất bản.'}
  ],
  history:[]
};
let db=loadDB(),session=null,currentTest=null;
function loadDB(){const raw=localStorage.getItem(DB_KEY);if(!raw){localStorage.setItem(DB_KEY,JSON.stringify(seed));return structuredClone(seed)}try{return JSON.parse(raw)}catch{return structuredClone(seed)}}
function save(){localStorage.setItem(DB_KEY,JSON.stringify(db))}
function $(q){return document.querySelector(q)}
function $all(q){return [...document.querySelectorAll(q)]}
function shuffle(arr){return arr.map(v=>[Math.random(),v]).sort((a,b)=>a[0]-b[0]).map(v=>v[1])}
function slugify(str){return (str||'bai-hoc').toLowerCase().normalize('NFD').replace(/[\u0300-\u036f]/g,'').replace(/đ/g,'d').replace(/[^a-z0-9]+/g,'-').replace(/^-+|-+$/g,'')||'bai-hoc'}
function escapeHtml(str){return String(str||'').replace(/[&<>'"]/g,s=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[s]))}
function getStatus(r){const vals=[r.ai,r.parent,r.admin].filter(Boolean); if(vals.length<3) return {key:'yellow',label:'VÀNG • Chờ đủ 3 nguồn xác nhận'}; const allSame=vals.every(v=>v===vals[0]); if(allSame) return {key:'green',label:'XANH • Đã thống nhất'}; return {key:'red',label:'ĐỎ • Xung đột đáp án, cần kiểm tra tận cùng'} }
function populateProfiles(){const role=$('#roleSelect')?.value||'student';const sel=$('#profileSelect');if(!sel)return;sel.innerHTML='';if(role==='student'){db.students.forEach(s=>sel.add(new Option(`${s.name} • ${s.grade}`,s.id)))}else if(role==='parent'){sel.add(new Option('Phụ huynh Thiên Kim','parent_001'))}else{sel.add(new Option('Admin / Giáo viên','admin_001'))}}
function initHome(){populateProfiles();$('#roleSelect')?.addEventListener('change',populateProfiles);$all('[data-role]').forEach(b=>b.onclick=()=>{$('#roleSelect').value=b.dataset.role;populateProfiles();$('#loginBox').scrollIntoView({behavior:'smooth'})});$('#loginBtn')?.addEventListener('click',()=>{const role=$('#roleSelect').value,pin=$('#pinInput').value.trim(),profile=$('#profileSelect').value;if(role==='student'){const st=db.students.find(s=>s.id===profile&&s.pin===pin);if(!st)return alert('Sai mã PIN học sinh. Demo dùng 1234.');session={role,studentId:st.id,name:st.name};renderStudent()}else if(role==='parent'){if(pin!=='8888')return alert('PIN phụ huynh demo: 8888');session={role,name:'Phụ huynh'};renderParent()}else{if(pin!=='9999')return alert('PIN admin demo: 9999');session={role,name:'Admin'};renderAdmin()}})}
function logout(){location.reload()}
function renderStudent(){const app=$('#app');app.innerHTML=$('#studentTpl').innerHTML;$('#logoutBtn').onclick=logout;$('#studentName').textContent=session.name;$('#testList').innerHTML=db.tests.map(t=>`<div class="test-item"><div><b>${t.title}</b><br><small>${t.duration} phút • đảo câu hỏi/đáp án • tự nộp khi hết giờ</small></div><button class="primary" onclick="startTest('${t.id}')">Bắt đầu</button></div>`).join('');const hs=db.history.filter(h=>h.studentId===session.studentId);const avg=hs.length?Math.round(hs.reduce((s,h)=>s+h.score,0)/hs.length):0;$('#studentStats').innerHTML=`<div class="score">${avg}/100</div><p>Điểm trung bình</p><p>Đã làm: <b>${hs.length}</b> bài</p><p>Nhắc nhớ: học để hiểu, không học vẹt theo thứ tự đáp án.</p>`;$('#lessonList').innerHTML=db.lessons.map(l=>`<div class="lesson-item"><div><b>${l.title}</b><br><small>${l.cat} • ${l.level}</small><br><code>${l.path}</code><p>${l.desc}</p></div></div>`).join('')}
function startTest(id){const test=db.tests.find(t=>t.id===id);let qs=test.questions.map(qid=>db.questions.find(q=>q.id===qid)).filter(Boolean);if(test.shuffleQuestions)qs=shuffle(qs);qs=qs.map(q=>({...q,displayOptions:test.shuffleOptions?shuffle(q.options):q.options}));currentTest={test,qs,idx:0,answers:{},left:test.duration*60,start:Date.now(),timer:null};$('#app').innerHTML=$('#testTpl').innerHTML;$('#backStudent').onclick=renderStudent;$('#testTitle').textContent=test.title;$('#prevQ').onclick=()=>{if(currentTest.idx>0){currentTest.idx--;drawQ()}};$('#nextQ').onclick=()=>{if(currentTest.idx<currentTest.qs.length-1){currentTest.idx++;drawQ()}};$('#submitTest').onclick=()=>submitTest('manual');drawQ();tick(true);currentTest.timer=setInterval(tick,1000)}
function drawQ(){const q=currentTest.qs[currentTest.idx];$('#progress').innerHTML=`Câu ${currentTest.idx+1}/${currentTest.qs.length} • <span class="pill">${q.topic}</span>`;$('#questionText').textContent=q.question;$('#options').innerHTML=q.displayOptions.map(o=>`<button class="option ${currentTest.answers[q.id]===o.id?'selected':''}" onclick="choose('${q.id}','${o.id}')"><b>${o.id}.</b> ${o.text}</button>`).join('')}
function choose(qid,oid){currentTest.answers[qid]=oid;drawQ()}
function tick(skipDec=false){if(!skipDec)currentTest.left--;const m=Math.floor(currentTest.left/60),s=currentTest.left%60;$('#timer').textContent=`${String(m).padStart(2,'0')}:${String(s).padStart(2,'0')}`;if(currentTest.left===300)alert('⏰ Còn 5 phút nữa! Con hãy kiểm tra lại các câu chưa làm nhé.');if(currentTest.left<=0)submitTest('timeout')}
function submitTest(type){clearInterval(currentTest.timer);let correct=0,wrong=0,blank=0;currentTest.qs.forEach(q=>{const a=currentTest.answers[q.id];if(!a)blank++;else if(a===q.correct)correct++;else wrong++});const score=Math.round(correct/currentTest.qs.length*100);const used=Math.round((Date.now()-currentTest.start)/1000);const focus=Math.max(40,Math.min(100,Math.round(100-(wrong*8)-(blank*12)-(type==='timeout'?15:0))));db.history.push({id:'his_'+Date.now(),studentId:session.studentId,testId:currentTest.test.id,score,correct,wrong,blank,used,submitType:type,focusScore:focus,date:new Date().toISOString()});save();$('#app').innerHTML=`<section class="card"><h2>🎉 Con đã hoàn thành bài test!</h2><div class="score">${score}/100</div><p>Đúng: <b class="good">${correct}</b> • Sai: <b class="bad">${wrong}</b> • Bỏ trống: <b>${blank}</b></p><p>Thời gian: ${Math.floor(used/60)} phút ${used%60} giây • Trạng thái: ${type==='timeout'?'Tự nộp khi hết giờ':'Nộp thủ công'}</p><h3>Nhận xét AI demo</h3><p>${score>=80?'Con làm rất tốt, hãy tiếp tục giữ tinh thần học hỏi nhé.':score>=50?'Con đã cố gắng, nên đọc kỹ đề hơn và ôn lại câu sai.':'Con cần luyện lại bài học này, mỗi ngày một chút sẽ tiến bộ.'}</p><button class="primary" onclick="renderStudent()">Quay về khu học sinh</button></section>`}
function renderParent(){const app=$('#app');app.innerHTML=$('#parentTpl').innerHTML;$('#logoutBtn').onclick=logout;const total=db.history.length,avg=total?Math.round(db.history.reduce((s,h)=>s+h.score,0)/total):0;$('#parentOverview').innerHTML=`<div class="row"><span>Tổng lượt làm bài</span><b>${total}</b></div><div class="row"><span>Điểm trung bình lớp</span><b>${avg}/100</b></div><div class="row"><span>Số học sinh</span><b>${db.students.length}</b></div>`;renderLeaderboard();$('#routeBtn').onclick=routeDraft;routeDraft()}
function renderLeaderboard(){const map={};db.history.forEach(h=>{map[h.studentId]=map[h.studentId]||[];map[h.studentId].push(h)});const rows=db.students.map(s=>{const hs=map[s.id]||[];const avg=hs.length?Math.round(hs.reduce((a,b)=>a+b.score,0)/hs.length):0;return {name:s.name,avg,count:hs.length}}).sort((a,b)=>b.avg-a.avg||b.count-a.count);$('#leaderboard').innerHTML=rows.map((r,i)=>`<div class="row"><span>${i+1}. ${r.name}<br><small>${r.count} lượt làm</small></span><b>${r.avg}/100</b></div>`).join('')}
function routeDraft(){const subject=$('#routeSubject').value,grade=$('#routeGrade').value,title=$('#routeTitle').value.trim(),content=$('#routeContent').value.trim();const rm=routerMap[subject];const slug=slugify(title);const path=rm.path.replace('/lop-3/',`/${grade}/`)+slug+'/';const aiAns=subject==='math'?'A':subject==='life_skill'?'C':'B';$('#routeResult').innerHTML=`<div class="route-box"><h3>✅ Kết quả phân loại demo</h3><div class="row"><span>Chuyên trang</span><b>${rm.portal}</b></div><div class="row"><span>Module</span><b>${rm.module}</b></div><div class="row"><span>Thư mục/đường dẫn đề xuất</span><code>${path}</code></div><div class="row"><span>Trạng thái</span><b class="warn">Nháp • Chờ duyệt</b></div><h4>Bản nháp câu hỏi AI</h4><p><b>Câu hỏi:</b> ${escapeHtml(content)}</p><p><b>A.</b> 117 &nbsp; <b>B.</b> 127 &nbsp; <b>C.</b> 137 &nbsp; <b>D.</b> 147</p><p><b>Đáp án AI gợi ý:</b> ${aiAns}</p><p><b>Lưu ý:</b> đây mới là bản nháp. Cần qua màn duyệt 3 nguồn: AI / phụ huynh-giáo viên / admin trước khi giao cho bé.</p></div>`}
function renderAdmin(){const app=$('#app');app.innerHTML=$('#adminTpl').innerHTML;$('#logoutBtn').onclick=logout;$('#adminStudents').innerHTML=db.students.map(s=>`<div class="row"><span>${s.name}<br><small>${s.grade} • PIN ${s.pin}</small></span></div>`).join('');$('#routeMap').innerHTML=Object.entries(routerMap).map(([k,v])=>`<div class="row"><span>${v.label}<br><small>${v.portal}</small></span><code>${v.path}</code></div>`).join('');renderReviews();$('#historyTable').innerHTML=db.history.length?db.history.map(h=>{const s=db.students.find(x=>x.id===h.studentId);return `<div class="row"><span>${s?.name||h.studentId}<br><small>${new Date(h.date).toLocaleString('vi-VN')} • ${h.submitType}</small></span><b>${h.score}/100</b></div>`}).join(''):'Chưa có lịch sử.'}
function renderReviews(){const html=db.reviews.map(r=>{const st=getStatus(r);return `<div class="review ${st.key}"><div class="review-head"><div><b>${escapeHtml(r.title)}</b><br><small>${escapeHtml(r.question)}</small><br><code>${r.path}</code></div><span class="status ${st.key}">${st.label}</span></div><div class="answer-grid"><label>AI<input data-id="${r.id}" data-field="ai" value="${r.ai||''}" maxlength="1"></label><label>Phụ huynh/GV<input data-id="${r.id}" data-field="parent" value="${r.parent||''}" maxlength="1"></label><label>Admin/Tôi<input data-id="${r.id}" data-field="admin" value="${r.admin||''}" maxlength="1"></label><label>Đáp án assistant<input value="${r.assistant||''}" disabled></label></div><p><b>Ghi chú:</b> ${escapeHtml(r.note)}</p></div>`}).join('');$('#reviewTable').innerHTML=html;document.querySelectorAll('#reviewTable input:not([disabled])').forEach(inp=>inp.onchange=(e)=>{const id=e.target.dataset.id,field=e.target.dataset.field;const r=db.reviews.find(x=>x.id===id);r[field]=e.target.value.trim().toUpperCase();save();renderReviews()})}
initHome();
window.startTest=startTest;window.choose=choose;window.renderStudent=renderStudent;
