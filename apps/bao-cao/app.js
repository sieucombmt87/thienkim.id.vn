function make(){let d=new Date().toLocaleString('vi-VN');result.textContent=`${title.value}\nThời gian: ${d}\n\nNội dung:\n${content.value}\n\nGhi chú:\n${note.value}`}makeBtn.onclick=make;copyBtn.onclick=()=>navigator.clipboard.writeText(result.textContent);saveBtn.onclick=()=>{let h=JSON.parse(localStorage.tk_baocao||'[]');h.unshift({t:new Date().toLocaleString('vi-VN'),v:result.textContent});localStorage.tk_baocao=JSON.stringify(h.slice(0,20));render()};function render(){let h=JSON.parse(localStorage.tk_baocao||'[]');history.innerHTML=h.map(x=>`<div class='row'><span>${x.t}</span><button class='btn alt' onclick='navigator.clipboard.writeText(${JSON.stringify('${x.v}')})'>Copy</button></div>`).join('')}render();