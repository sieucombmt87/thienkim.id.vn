function arr(){return list.value.split(/\n/).map(x=>x.trim()).filter(Boolean)}function rnd(n){let a=arr();crypto.getRandomValues(new Uint32Array(a.length)).forEach((v,i)=>a[i]={v,x:a[i]});a.sort((p,q)=>p.v-q.v);return a.slice(0,n).map(x=>x.x)}pickBtn.onclick=()=>{result.textContent=rnd(+count.value||1).map((x,i)=>`${i+1}. ${x}`).join('\n')};copyBtn.onclick=()=>navigator.clipboard.writeText(result.textContent);shuffleBtn.onclick=()=>{list.value=rnd(arr().length).join('\n')};