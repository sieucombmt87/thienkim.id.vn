function money(n){return Number(n||0).toLocaleString('vi-VN')}function calc(){let total=0,lines=[];document.getElementById('cashInput').value.split(/\n/).forEach(l=>{let [m,q]=l.trim().split(/\s+/);m=+m||0;q=+q||0;if(m){total+=m*q;lines.push(`${money(m)} x ${q} = ${money(m*q)}`)}});let sys=+(document.getElementById('systemMoney').value||'').replace(/\D/g,'')||0;let diff=total-sys;document.getElementById('result').textContent=lines.join('\n')+`\n\nTổng đếm: ${money(total)}\nHệ thống: ${money(sys)}\nChênh lệch: ${money(diff)}`}document.getElementById('calcBtn').onclick=calc;document.getElementById('copyBtn').onclick=()=>navigator.clipboard.writeText(document.getElementById('result').textContent);document.getElementById('clearBtn').onclick=()=>{document.getElementById('systemMoney').value='';document.getElementById('result').textContent='Chưa tính.'}